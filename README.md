# test01
use: <br/>
open Fmodel, setting - mapping:<br/>
Endpoint: `https://raw.githubusercontent.com/555me/NTE-usmap/refs/heads/main/index.json` <br/>
Expression: `$.mapping[0]['url', 'name']`<br/>

if u use os client, u can change 0 to 1 maybe.
